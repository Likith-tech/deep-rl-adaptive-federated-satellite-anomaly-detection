# Dataset configuration (placeholder — populated in Phase 1)
#
# This file will define which dataset(s) are used for anomaly detection
# experiments, where they are stored, and how they are split.

dataset:
  name: null              # e.g. "CICIDS2017", "UNSW-NB15", "NSL-KDD"
  source_url: null
  raw_dir: data/raw
  interim_dir: data/interim
  processed_dir: data/processed
  partitions_dir: data/partitions

split:
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  random_seed: 42

notes: >
  No dataset has been downloaded or selected yet. This file is a
  placeholder for Phase 1 (dataset acquisition and analysis).
