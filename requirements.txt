# ---- Phase 0 foundation dependencies ----
# Backend API
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.10.4
pydantic-settings>=2.7.1

# Testing
pytest>=8.3.4
httpx>=0.28.1

# Config handling
PyYAML>=6.0.2

# Dev tooling
python-dotenv>=1.0.1

# ---- Phase 1 dataset / preprocessing dependencies ----
# Flower, Gymnasium, etc. will be added when FL/DRL phases are implemented.
pandas>=2.2.3
numpy>=2.1.3
scikit-learn>=1.5.2
matplotlib>=3.9.2
pyarrow>=18.0.0
joblib>=1.4.2
scipy>=1.14.0

# ---- Phase 2 baseline model dependencies ----
# torch is intentionally NOT listed as a normal requirement here: a plain
# `pip install -r requirements.txt` would resolve it against the default
# PyPI index, which can pull a much larger CUDA-enabled build (or fail to
# resolve at all on a brand-new Python version). Install it explicitly
# with the small CPU-only build instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# (torch>=2.13.0 is the version this project was developed/tested against)
